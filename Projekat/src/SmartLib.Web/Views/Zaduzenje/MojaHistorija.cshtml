@model IEnumerable<ZaduzenjeViewModel>
@{
    ViewData["Title"] = "Historija mojih zaduženja";
    var lista = Model.OrderByDescending(z => z.DatumStvarnogVracanja).ToList();
}

<div class="katalog-header">
    <h2>Historija mojih zaduženja</h2>
    <a asp-action="Moja" class="btn btn-secondary">← Aktivna zaduženja</a>
</div>

<p class="katalog-meta">
    Zatvorena zaduženja iz <strong>posljednje 3 godine</strong> · ukupno: <strong>@lista.Count</strong>
</p>

@if (TempData["SuccessMessage"] is string successMsg)
{
    <div class="alert alert-success">@successMsg</div>
}
@if (TempData["ErrorMessage"] is string errorMsg)
{
    <div class="alert alert-error">@errorMsg</div>
}

@if (!lista.Any())
{
    <div class="card katalog-empty">
        <p>Nema zatvorenih zaduženja u posljednje 3 godine.</p>
    </div>
}
else
{
    <div class="card katalog-card">
        <div class="katalog-table-wrap">
            <table class="katalog-table">
                <thead>
                    <tr>
                        <th>Knjiga</th>
                        <th>Inv. br.</th>
                        <th>Zaduženo</th>
                        <th>Rok vraćanja</th>
                        <th>Vraćeno</th>
                        <th>Akcija</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var z in lista)
                    {
                        <tr>
                            <td data-label="Knjiga"><strong>@z.KnjigaNaslov</strong></td>
                            <td data-label="Inv. br." class="katalog-isbn">@z.InventarniBroj</td>
                            <td data-label="Zaduženo">@z.DatumZaduzivanja.ToLocalTime().ToString("dd.MM.yyyy")</td>
                            <td data-label="Rok vraćanja">@z.DatumPlaniranogVracanja.ToLocalTime().ToString("dd.MM.yyyy")</td>
                            <td data-label="Vraćeno">
                                @if (z.DatumStvarnogVracanja.HasValue)
                                {
                                    <span class="historija-vraceno">@z.DatumStvarnogVracanja.Value.ToLocalTime().ToString("dd.MM.yyyy")</span>
                                }
                                else
                                {
                                    <span class="historija-nepoznato">—</span>
                                }
                            </td>
                            <td data-label="Akcija">
                                @if (!z.DatumStvarnogVracanja.HasValue)
                                {
                                    <span class="historija-nepoznato">—</span>
                                }
                                else if (z.ImaRecenziju)
                                {
                                    <span class="katalog-badge" style="background: rgba(245,158,11,0.12); color: #f59e0b;">Recenzija ostavljena</span>
                                }
                                else
                                {
                                    <a asp-controller="Recenzija" asp-action="Dodaj" asp-route-knjigaId="@z.KnjigaId" class="btn btn-primary btn-sm">Ocijeni knjigu</a>
                                }
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
}
